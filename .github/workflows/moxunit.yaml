name: Test
on: push
jobs:
  run-moxunit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: joergbrech/moxunit-action@master
        with:
          tests: demo
          src: demo
